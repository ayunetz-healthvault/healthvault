{
  "name": "ayunetz-document-processing",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Development document-processing API for Ayunetz Health Vault. Synthetic data only.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "stack:up": "docker compose -f docker-compose.yml up -d --wait",
    "stack:down": "docker compose -f docker-compose.yml down",
    "stack:reset": "docker compose -f docker-compose.yml down -v && npm run stack:up",
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/server.js",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "test": "vitest run",
    "test:watch": "vitest",
    "verify": "npm run typecheck && npm run lint && npm run test"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.1113.0",
    "@aws-sdk/client-s3": "^3.1113.0",
    "@aws-sdk/client-sqs": "^3.1113.0",
    "@aws-sdk/lib-dynamodb": "^3.1113.0",
    "@aws-sdk/s3-request-presigner": "^3.1113.0",
    "@fastify/multipart": "^10.1.0",
    "fastify": "^5.2.1",
    "jose": "^5.10.0",
    "mupdf": "^1.28.0",
    "tesseract.js": "^7.0.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@eslint/js": "^9.18.0",
    "@types/node": "^22.10.7",
    "eslint": "^9.18.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "typescript-eslint": "^8.21.0",
    "vitest": "^3.0.4"
  }
}
